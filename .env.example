# Firebase Configuration
FIREBASE_SERVICE_ACCOUNT='{"type": "service_account", "project_id": "...", ...}'

# SMTP Configuration (Optional, defaults to Gmail if SMTP_HOST is missing)
# For Gmail, leave SMTP_HOST empty and use an App Password in the dashboard
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true

# AI Configuration
GEMINI_API_KEY=
